apiVersion: v1
kind: Service
metadata:
  name: {{ include "graylog-auth-proxy.fullname" . }}
  labels:
    {{- include "graylog-auth-proxy.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "graylog-auth-proxy.selectorLabels" . | nindent 4 }}
